import { Outlet } from "react-router-dom";

export default function Main(){


    return (
        <main>
            <section>
                
            </section>
            <Outlet/>
        </main>
    )
}